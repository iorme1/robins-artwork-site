<div class="poems-index">
  <% @poems.each do |poem| %>
    <div class="poems-display">
      <h1 id="poems-title"><strong><%= link_to poem.title, poem_path(poem) %></strong></h1><br/>
    </div>
  <% end %>
</div>
